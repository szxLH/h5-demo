{
  "name": "zhikemi",
  "version": "1.0.0",
  "description": "",
  "main": "baobiao.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "fis3 server stop && fis3 server clean && fis3 server start && fis3 release -wL",
    "output": "fis3 release -d ./dist"
  },
  "author": "tomokylelie",
  "license": "ISC",
  "devDependencies": {
    "fis3-hook-relative": "^2.0.0",
    "fis3-parser-css": "^1.0.0",
    "fis3-parser-scss": "^1.0.5"
  }
}
